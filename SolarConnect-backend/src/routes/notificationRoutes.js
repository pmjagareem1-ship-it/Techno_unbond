import {Router} from "express"; import Notification from "../models/Notification.js"; import {auth} from "../middleware/authMiddleware.js"; const r=Router(); r.get("/",auth,async(q,s)=>s.json(await Notification.find({user:q.user.id}).sort({createdAt:-1}))); r.patch("/:id/read",auth,async(q,s)=>s.json(await Notification.findOneAndUpdate({_id:q.params.id,user:q.user.id},{read:true},{new:true}))); export default r;
