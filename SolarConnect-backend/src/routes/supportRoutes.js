import {Router} from "express"; import SupportTicket from "../models/SupportTicket.js"; import {auth} from "../middleware/authMiddleware.js"; const r=Router(); r.get("/my",auth,async(q,s)=>s.json(await SupportTicket.find({user:q.user.id}).sort({createdAt:-1}))); r.post("/",auth,async(q,s)=>s.status(201).json(await SupportTicket.create({...q.body,user:q.user.id}))); export default r;
