import {Router} from "express"; import bcrypt from "bcryptjs"; import jwt from "jsonwebtoken"; import crypto from "crypto"; import User from "../models/User.js";
const r=Router(); const token=u=>jwt.sign({id:u._id,role:u.role,name:u.name},process.env.JWT_SECRET,{expiresIn:"7d"});
r.post("/signup",async(req,res)=>{try{const{name,email,password,phone}=req.body;if(!name||!email||!password)return res.status(400).json({message:"name, email and password are required"});if(await User.findOne({email}))return res.status(409).json({message:"Email already registered"});const u=await User.create({name,email,phone,password:await bcrypt.hash(password,12),referralCode:`SOLAR-${crypto.randomBytes(4).toString("hex").toUpperCase()}`});res.status(201).json({user:{id:u._id,name:u.name,email:u.email},token:token(u)})}catch(e){res.status(500).json({message:e.message})}});
r.post("/login",async(req,res)=>{try{const{email,password}=req.body,u=await User.findOne({email});if(!u||!(await bcrypt.compare(password,u.password)))return res.status(401).json({message:"Invalid credentials"});res.json({user:{id:u._id,name:u.name,email:u.email,role:u.role},token:token(u)})}catch(e){res.status(500).json({message:e.message})}}); export default r;
